services:
  ml_worker:
    build: ./ml_node
    restart: unless-stopped
    environment:
      MONGO_URI: mongodb://192.168.1.59:27017/app
      API_TCP_ADDRESS: 192.168.1.59:9090
    deploy:
      mode: replicated
      replicas: 3